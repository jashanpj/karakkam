class PackageOption {

  String title;
  String oldrate, newrate;
  List<String> inclusive;
  List<String> exclusive;
  PackageOption(this.title, this.oldrate, this.newrate);
 // PackageOption(this.title, this.oldrate, this.newrate, this.inclusive, this.exclusive);

}